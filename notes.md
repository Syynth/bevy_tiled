need preludes for all the crates
need root crate with feature flags for all the stuff
  - export types
  - avian
  - rendering/tilemap
explore pre-processing functionality (don't do this yet, this is way later)
rename all the plugins, don't need Bevy* in front because that's dumb
